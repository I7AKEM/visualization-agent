# Renderer registry scaffold

This package deliberately contains no renderer registrations in WP-01. Its
peer ranges encode the audited UI compatibility trains without beginning WP-02
or later product implementation.
