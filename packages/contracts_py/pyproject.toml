[project]
name = "visualization-agent-contracts"
version = "0.0.0"
description = "Empty Python contract package; WP-02 owns generated contracts."
requires-python = "==3.12.*"
dependencies = ["pydantic==2.13.5"]

[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/visualization_agent_contracts"]
