[project]
name = "visualization-agent-evals"
version = "0.0.0"
description = "Empty evaluation package; WP-06 owns evaluation behavior and corpora."
requires-python = "==3.12.*"
dependencies = [
  "logfire==4.41.0",
  "pydantic-evals==2.36.0",
]

[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/visualization_agent_evals"]
