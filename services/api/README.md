# API service scaffold

WP-01 creates only an importable package boundary. Identity, storage, model routes,
commands, streaming endpoints, and all other product behavior remain disabled until
their owning work packages and owner inputs are accepted.
